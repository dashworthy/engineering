---
type: tool_used
tool: Skill
input_match: '"skill":\s*"(?:[\w-]+:)?triage"'
min: 0
max: 0
arm: both
---
The wrong-door guard, scored in both arms: a feature ask must NOT be routed to `triage` (the
defect entrance). Passes when `triage` never fired.
